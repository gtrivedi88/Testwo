Executing task runs and pipeline runs in Kubernetes requires secure authentication. Use the following recommended approaches to handle authentication while executing task runs and pipeline runs in GitLab, GitHub, or Docker using git secrets:

Using secrets you can authenticate to GitLab, GitHub, or Docker by:

* Using Secrets and Workspaces in Tekton Pipelines
* Using a ServiceAccount
